{
  "name": "nyc-subway-3d",
  "version": "1.0.0",
  "description": "Live 3D visualization of the NYC subway system",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "gtfs": "bash scripts/download-gtfs.sh"
  },
  "dependencies": {
    "gtfs-realtime-bindings": "^2.0.0",
    "three": "^0.165.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "vite": "^5.3.1",
    "vitest": "^1.6.0"
  }
}
