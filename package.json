{
  "name": "nyc-subway-3d",
  "version": "1.0.0",
  "description": "Live 3D visualization of the NYC subway system",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "predev": "bash scripts/download-gtfs.sh",
    "prebuild": "bash scripts/download-gtfs.sh",
    "build": "vite build && node scripts/verify-build.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "gtfs": "bash scripts/download-gtfs.sh --force"
  },
  "dependencies": {
    "maplibre-gl": "^5.24.0",
    "three": "^0.165.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "vite": "^5.3.1",
    "vitest": "^1.6.0"
  }
}
